<% provide(:title, @title) %>
<div class="row">
  <aside class="span4">
    <section>
      <div class="main-avatar"><%= image_tag @user.avatar, size: "210x210" %> </div>
        <div>
          <ul class="pull-left inline follow-btn">
            <li><h1><%= @user.full_name %></h1></li>
            <li><%= render 'follow_form' if signed_in? %></li>
            <li><% unless !current_user?(@user) %> 
                  <%= link_to "Edit", edit_user_path, class: "btn btn-medium"  %> 
                <% end %> 
            </li>
          </ul>
        </div>
    </section>
    <section>
      <%= render 'shared/stats' %>

    </section>
  </aside>
  <div class="span8">
    <h1><%= @title %></h1>
 
  </div>
</div>


