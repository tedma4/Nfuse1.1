<% case post.type %>
  <% when 'photo' %>
    <div class="timeline-user-box">
      <%= image_tag(post.image, :class => "gplus_image") %>
    </div>
    <% if post.text.present? %>
      <div class="center">
        <%= post.text %>
      </div>
    <% end %>
  <% when 'video' %>
    <div class="timeline-user-box">
      <iframe
      id="ytplayer"
      type="text/html"
      width="100%"
      height="225"
      src="<%= post.video %>"
      frameborder="0"
      allowfullscreen>
      </iframe>
    </div>
    <% if post.text.present? %>
      <div class="center">
        <%= post.text %>
      </div>
    <% end %>
<% else %>
  <div class="center text_only_post">
    <%= post.text %>
  </div>
<% end %>

