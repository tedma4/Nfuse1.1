<% if !shout.url.blank? %>
  <p> <%= shout.url_html %></p>
  <div class="url_caption">
    <%= shout.content %>
    <!-- <p>Duration: <b><%#= shout.duration %></b></p> -->
  </div>
<% elsif shout.is_pic? %>
  <div class="timeline-user-box image_post">
    <%= image_tag shout.pic_url %>
  </div>
  <div class="caption">
      <%= shout.content %>
  </div>
<% elsif shout.is_video? %>
  <div class="timeline-user-box">
    <video 
      poster='transparent' 
      style="background:transparent url(<%= shout.pic_url %>) no-repeat 50% 50%;"
      preload="none"
      id="ytplayer" 
      type="video/mp4" 
      width="100%" 
      height="225" 
      src="<%= shout.video_url %>" 
      frameborder="0"
      allowfullscreen
      controls>
    </video>
    <%#= videojs_rails sources: { mp4: "http://domain.com/path/to/video.mp4", webm: "http://another.com/path/to/video.webm"}, setup: "{}", controls: false, width:"350" %>
  </div>
  <div class="caption">
      <%= shout.content %>
  </div>
<% elsif shout.is_full_video? %>
  <div class="timeline-user-box">
    <video 
      poster='transparent' 
      style="background:transparent url(<%= shout.pic_url %>) no-repeat 50% 50%;"
      preload="none"
      id="ytplayer" 
      type="video/flv" 
      width="100%" 
      height="225" 
      src="<%= shout.full_video_url %>" 
      frameborder="0"
      allowfullscreen
      controls>
    </video>
    <%#= videojs_rails sources: { mp4: "http://domain.com/path/to/video.mp4", webm: "http://another.com/path/to/video.webm"}, setup: "{}", controls: false, width:"350" %>
  </div>
  <div class="caption">
    <%= shout.content %>
  </div>
<% end %>